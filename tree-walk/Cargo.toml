[package]
name = "tree-walk"
version = "0.1.0"
edition = "2024"

[dependencies]
lexer = { path = "../lexer" }
report = { path = "../report" }
thiserror = "2"
anyhow = "1"
derive_more = { version = "2", features = ["from"] }
slotmap = "1"
